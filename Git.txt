git init----to initializing the git
git add-- to add the files to the staging area(git add filename)
git commit -- to add the files to local repository(git commint -m "User defined message")
git status---to check the status 
git push---to push the files to the cloud repository

git log --- information  about the files we commit

git add ., git add -A

git show id(in the log command)



git remote add origin https://github.com/Prasad3916/10KC.git
git branch -M main
git push -u origin main